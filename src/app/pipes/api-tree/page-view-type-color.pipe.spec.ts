import { PageViewTypeColorPipe } from './page-view-type-color.pipe';

describe('PageViewTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PageViewTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
