import { ApiTypeColorPipe } from './api-type-color.pipe';

describe('ApiTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ApiTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
