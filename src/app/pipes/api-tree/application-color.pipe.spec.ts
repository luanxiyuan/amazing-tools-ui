import { ApplicationColorPipe } from './application-color.pipe';

describe('ApplicationColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationColorPipe();
    expect(pipe).toBeTruthy();
  });
});
