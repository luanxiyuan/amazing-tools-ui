import { UiAppTypeColorPipe } from './ui-app-type-color.pipe';

describe('UiAppTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new UiAppTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
