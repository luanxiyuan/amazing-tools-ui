import { PrStateColorPipe } from './pr-state-color.pipe';

describe('PrStateColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PrStateColorPipe();
    expect(pipe).toBeTruthy();
  });
});
